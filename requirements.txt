# Dependencias del analisis y el reporte.  Instalar con:  pip install -r requirements.txt
neo4j>=5.0
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
networkx>=3.0
jupyter>=1.0
nbconvert>=7.0

# Opcional, solo para exportar el reporte a PDF con `jupyter nbconvert --to webpdf`:
#   playwright>=1.40   (despues correr: python -m playwright install chromium)
